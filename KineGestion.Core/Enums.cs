namespace KineGestion.Core
{
    public enum SessionStatus
    {
        Pending = 0,
        Completed = 1,
        Canceled = 2
    }

    public enum PaymentStatus
    {
        Pending = 0,
        Paid = 1
    }
}