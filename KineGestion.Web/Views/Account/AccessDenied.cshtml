@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="kg-page-header">
    <div>
        <h2>Acceso denegado</h2>
        <p class="text-muted mb-0">No tenés permisos para ver esta página</p>
    </div>
</div>

<div class="kg-card text-center" style="padding: 3rem">
    <div style="font-size:3rem;color:#ef4444;margin-bottom:1rem"><i class="bi bi-shield-x-fill"></i></div>
    <h4>Sin autorización</h4>
    <p class="text-muted">Tu rol no tiene acceso a esta sección del sistema.</p>
    <a asp-controller="Home" asp-action="Index" class="btn-kg-primary mt-2">
        <i class="bi bi-house-fill"></i> Volver al Dashboard
    </a>
</div>
