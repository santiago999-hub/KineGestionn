@model IEnumerable<KineGestion.Web.Models.ViewModels.PatientViewModel>

@{
    ViewData["Title"] = "Pacientes";
}

<div class="kg-page-header">
    <div>
        <h2>Pacientes</h2>
        <p class="text-muted mb-0">Gestión del padrón de pacientes activos</p>
    </div>
    <a asp-action="Create" class="btn-kg-primary">
        <i class="bi bi-person-plus-fill"></i> Nuevo Paciente
    </a>
</div>

<div class="kg-card">
    <div class="kg-card-header">
        <h5 class="kg-card-header-title">
            <i class="bi bi-people-fill"></i> Listado de Pacientes
        </h5>
        <span class="kg-badge kg-badge-muted">@Model.Count() registros</span>
    </div>

    @if (!Model.Any())
    {
        <div class="kg-empty">
            <div class="kg-empty-icon"><i class="bi bi-people"></i></div>
            <h5>Sin pacientes registrados</h5>
            <p>Todavía no hay pacientes en el sistema.</p>
            <a asp-action="Create" class="btn-kg-primary">
                <i class="bi bi-person-plus-fill"></i> Registrar el primero
            </a>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="kg-table">
                <thead>
                    <tr>
                        <th>Paciente</th>
                        <th>DNI</th>
                        <th>Obra Social</th>
                        <th>Teléfono</th>
                        <th>Estado</th>
                        <th style="text-align:right">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var p in Model)
                    {
                        <tr>
                            <td>
                                <div style="display:flex;align-items:center;gap:10px">
                                    <div style="width:34px;height:34px;border-radius:50%;background:#dbeafe;color:#2563eb;display:flex;align-items:center;justify-content:center;font-size:.8rem;font-weight:700;flex-shrink:0">
                                        @(p.Nombre.Length > 0 ? p.Nombre[0].ToString().ToUpper() : "?")@(p.Apellido.Length > 0 ? p.Apellido[0].ToString().ToUpper() : "")
                                    </div>
                                    <div>
                                        <div style="font-weight:600">@p.Apellido, @p.Nombre</div>
                                    </div>
                                </div>
                            </td>
                            <td><span style="font-family:monospace;font-size:.82rem">@p.DNI</span></td>
                            <td>@(string.IsNullOrEmpty(p.ObraSocial) ? "—" : p.ObraSocial)</td>
                            <td>@(string.IsNullOrEmpty(p.Telefono) ? "—" : p.Telefono)</td>
                            <td>
                                @if (p.IsActivo)
                                {
                                    <span class="kg-badge kg-badge-success"><i class="bi bi-circle-fill" style="font-size:.45rem"></i> Activo</span>
                                }
                                else
                                {
                                    <span class="kg-badge kg-badge-muted">Inactivo</span>
                                }
                            </td>
                            <td>
                                <div class="kg-action-btns" style="justify-content:flex-end">
                                    <a asp-action="Details" asp-route-id="@p.Id" class="kg-action-btn view" title="Ver detalle"><i class="bi bi-eye-fill"></i></a>
                                    <a asp-action="Edit" asp-route-id="@p.Id" class="kg-action-btn edit" title="Editar"><i class="bi bi-pencil-fill"></i></a>
                                    <a asp-action="Delete" asp-route-id="@p.Id" class="kg-action-btn del" title="Dar de baja"><i class="bi bi-trash-fill"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div style="padding:12px 18px;border-top:1px solid var(--kg-border);font-size:.76rem;color:var(--kg-text-muted)">
            Mostrando @Model.Count() pacientes activos
        </div>
    }
</div>
